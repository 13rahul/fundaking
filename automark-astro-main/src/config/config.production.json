{
  "site": {
    "base_url": "https://fundaking.com",
    "base_path": "/"
  },
  "_note": "Copy site.base_url and site.base_path into config.json before npm run build:main for production deploy. Set google_site_verification and google_tag_manager.gtm_id in config.json when ready; keep enable false until GTM ID is set."
}
